@every_4_guesses.each_with_index { |guess_element, guess_index|
    #   @code.each_with_index { |code_element, code_index|
    #     if guess_element == code_element && guess_index == code_index
    #       @guess_feedback << "R"
    #     end
    #   }
    # }